package org.bitcoin.timechain.calendar;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
// public class StellantisFormsProductBoilerplateJetty extends SpringBootServletInitializer {
public class BitcoinTimechainCalendarApplication {

    public static void main(String[] args) {
        // new SpringApplicationBuilder(StellantisFormsProductBoilerplateJetty.class).web(WebApplicationType.SERVLET).run(args);
        SpringApplication.run(BitcoinTimechainCalendarApplication.class, args);
    }

}
